# Huantuo Restaurant Growth OS — Updates

This public repository contains Windows release binaries and automatic-update metadata only.

- `HuantuoRestaurantGrowthOS-*-Setup.exe`: Windows x64 installer
- `*.blockmap`: differential-update metadata
- `latest.yml`: Electron update manifest

Application source code is maintained separately in a private repository.